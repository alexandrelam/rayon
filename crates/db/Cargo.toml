[package]
name = "rayon-db"
version.workspace = true
edition.workspace = true

[dependencies]
rayon-types = { path = "../types" }
sonic-channel = { workspace = true, features = ["search", "ingest"] }
