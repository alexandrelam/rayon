[package]
name = "rayon-core"
version.workspace = true
edition.workspace = true

[dependencies]
rayon-db = { path = "../db" }
rayon-platform = { path = "../platform" }
rayon-types = { path = "../types" }
serde.workspace = true
toml = "0.8"
