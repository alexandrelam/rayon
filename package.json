{
  "name": "rayon",
  "private": true,
  "scripts": {
    "check": "pnpm lint && pnpm typecheck && cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings -W clippy::unwrap_used -W clippy::expect_used -W clippy::panic -W clippy::todo -W clippy::dbg_macro",
    "dev": "pnpm --dir apps/desktop dev",
    "build": "pnpm --dir apps/desktop build",
    "format": "pnpm exec biome format --write apps/desktop/src apps/desktop/vite.config.ts eslint.config.mjs",
    "format:check": "pnpm exec biome format --check apps/desktop/src apps/desktop/vite.config.ts eslint.config.mjs",
    "hooks:install": "pnpm exec lefthook install",
    "lint": "pnpm --dir apps/desktop lint && cargo clippy --workspace --all-targets -- -D warnings -W clippy::unwrap_used -W clippy::expect_used -W clippy::panic -W clippy::todo -W clippy::dbg_macro",
    "lint:audit": "pnpm --dir apps/desktop lint && cargo clippy --workspace --all-targets -- -W clippy::pedantic",
    "prepare": "pnpm run hooks:install",
    "tauri": "pnpm --dir apps/desktop tauri",
    "test": "pnpm --dir apps/desktop test && cargo test --workspace",
    "typecheck": "pnpm --dir apps/desktop typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.13",
    "@eslint/js": "^9.39.4",
    "eslint": "^9.39.4",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "eslint-plugin-vitest": "^0.5.4",
    "globals": "^17.5.0",
    "lefthook": "^2.1.6",
    "typescript-eslint": "^8.59.0"
  }
}
